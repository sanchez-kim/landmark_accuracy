{
  "name": "landmark_accuracy",
  "version": "1.0.0",
  "description": "Determine 3D mesh's accuracy by calculating landmark distance",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "start": "electron .",
    "package": "electron-packager . landmark_tool --overwrite --platform=win32 --arch=x64 --prune=true --out=release-builds --icon=assets/insighter.ico"
  },
  "author": "Sanchez Kim",
  "license": "ISC",
  "dependencies": {
    "electron": "^27.0.0",
    "electron-package": "^0.1.0",
    "three": "^0.157.0",
    "vite": "^4.4.11"
  }
}
